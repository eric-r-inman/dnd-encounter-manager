Take on the role of an experienced Javascript and Rust programmer who has been hired to refactor a working project.

You will be refactoring this app into a new project architecture. 

In /dev/rust-template/ is the template we will be using. The README.org document in that folder will give you the details you need.

When you're done, the refactored app logic should work perfectly, and the refactored app UI should be identical, as the current iteration. You are resposible for ensuring every feature makes it into the refactored app. You are responsible for all testing during the refector.

Ensure that the original project is backed up on git, so that it can be reverted to if needed.

Consideration: I do not know the state of the documentation in this project folder. The documentation is likely to be somewhat, or even largely, deprecated. But, the app itself works, so refer to the project code when determining features.

Now, read the README.org file in /dev/rust-template/, and using this new template, refactor dnd-encounter-manager.

---

Please scan the documentation for this project and ensure it's up-to-date. I don't know if the dev-facing and user-facing documentation has been updated to reflect project state, etc.

---

How much of the app's logic is handled by JS, and how much by Rust? And, how much logic being handled by JS currently could be refactored to Rust?